# First end-to-end run — captured artifacts

Produced 2026-09-17 by a real `claude --agent root-architect` process against a
throwaway workspace repository, from the installed `dist/claude-code` bundle.
Not a fixture and not written by hand: every file here is what root actually
wrote. The run stopped partway (the agent process hit its own session limit),
which is why the mailbox is unsealed — see [[First End-to-End Run]] for what
that proves rather than what it costs.

Verbatim except for one thing: nothing was edited, and nothing was added.
