{
  "id": "orchestrator",
  "kind": "orchestrator",
  "role": "orchestrator",
  "description": "Runs one dispatch to completion on root's behalf. Delegates each task to a worker subagent, reads what comes back and judges it, decides what to delegate next, and asks root through the mailbox when it needs something only root can give. Never edits source, never commits, never speaks to the operator directly. Returns the orchestrator outcome as a single fenced JSON block.",
  "mutation": "read-and-run",
  "model": { "default": "strong", "escalation": [] },
  "reasoning": { "default": "high", "escalation": [] },
  "tools": {
    "allow": ["read-files", "search-files", "run-commands", "delegate"],
    "deny": ["edit-files", "create-files"],
    "shell_purpose": "Drive the job queue and the mailbox: dispatch_state.py, mailbox.py, check_return.py. Not for editing, not for Git, not for exploration - reading is what read-files and search-files are for."
  },
  "must_not": ["commit", "stage", "widen-scope", "ask-owner", "edit-files", "plan-work"],
  "returns": "orchestrator-outcome.schema.json",
  "prose": "references/agents/orchestrator.md"
}
